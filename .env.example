# Copy to .env and fill in your key. Do not commit .env.
OPENAI_API_KEY=your_openai_key_here

# Optional: model (default gpt-4o). Use gpt-4o-mini for lower cost.
# OPENAI_MODEL=gpt-4o

# Optional: RAG for AI endpoints. Indexes project docs and injects relevant chunks into prompts.
# RAG_ENABLED=1
